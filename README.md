# json-parse

Streaming JSON parser that yields valid partial objects as data arrives. Includes a browser demo with a Zod schema panel, step-through debugger, and incomplete key suppression.

Open `index.html` in a browser to try it.
